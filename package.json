{
  "name": "cpe-wtpp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "reset": "yarn cache clean && rm -rf node_modules && rm -rf yarn.lock&& yarn install",
    "clean:tokens": "rm -rf tokens",
    "dev": "node src/index.js",
    "dev:ts": "npm run clean:tokens && ts-node-dev --respawn --transpile-only --ignore-watch node_modules --no-notify src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "1.22.1"
  }
}
